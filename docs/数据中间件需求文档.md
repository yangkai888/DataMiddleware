1, 在Linux下使用go语言构建统一的数据中间件服务，同时为游戏服务器和网站前端及管理后台提供高并发、安全的数据访问能力, 用于访问玩家、角色、道具、订单、鉴权等业务数据,  前期以Oracle为底层数据库，后期平滑切换到MySQL;  
2, 对所有请求进行鉴权与权限控制, 游戏服务器使用自定义tcp协议访问数据中间件服务, 网站提供web服务。
3, 需要必要的日志记录,用于故障排查, 流量分析等
4, 我有多款游戏及对应的网站需要使用这个数据中间件服务，不同游戏使用不同数据库, 每款游戏的业务逻辑可能不同,  开发时候只需要支持一款, 保证可移植性, 后续我修改业务逻辑支持多款游戏